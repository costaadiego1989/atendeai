# API base URL
VITE_API_URL=http://localhost:3000

# Niche-based module filtering (default: enabled).
# Set to "false" to disable and show all modules for all tenants (useful for QA/dev).
VITE_FILTER_MODULES_BY_NICHE=true
